import java.time.LocalDate; // import the LocalDate class

public class Restaurant {

    //loads today's date to the variable
    LocalDate myObj = LocalDate.now();
    private String todayDate = new String(myObj.toString());





}
